// Byte.Literal.swift
//
// Namespace for byte-literal types — fixed byte sequences interpreted as
// literal patterns to match or emit. Lives in the `Byte.Literal` sub-domain
// per the institute's Domain.Subdomain naming convention: `Byte` is the
// byte-domain namespace; `Literal` is the byte-literal sub-concept.

extension Byte {
    /// A namespace for byte-literal types — fixed byte sequences interpreted as literal patterns to match or emit.
    public enum Literal {}
}
